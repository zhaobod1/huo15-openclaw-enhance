{
  "name": "@huo15/huo15-openclaw-enhance",
  "version": "6.6.5",
  "publishConfig": {
    "access": "public"
  },
  "description": "火一五·克劳德·龙虾增强插件 v6.6.5 — 上下文守护 hotfix 闭环 (v6.6.3/4/5)：(1) v6.6.4 修跨 provider 强切撞 400 mismatch — 新增 MODEL_TO_PROVIDER_MAP + readInstalledProviders 扫 agents.defaults.models 拿真实 provider，filter 三重过滤（KNOWN × providerKnown × providerInstalled），before_model_resolve 同时返 modelOverride+providerOverride 让 OpenClaw 切到正确 client，修用户实测『麻将观战可行性报告』→ Something went wrong 事故；(2) v6.6.3 cc-bridge-prompt 严格触发，普通对话不附 dashboard 链接 + 杜绝 /dashboard 残留；(3) v6.6.5 SKILL.md / package.json description 跟版本号同步（之前 SKILL.md 描述还说 v6.6.2 但 version 6.6.4）。继承 v6.6.2 runId 去重 + 静音工具 + v6.6.0 cost-aware + 多模态精算 + channel 差异化。",
  "type": "module",
  "main": "./dist/index.js",
  "openclaw": {
    "extensions": [
      "./index.ts"
    ],
    "build": {
      "openclawVersion": "2026.4.29"
    },
    "compat": {
      "pluginApi": ">=2026.4.24"
    }
  },
  "bin": {
    "openclaw-enhance-setup": "./scripts/setup.sh"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "release": "bash scripts/release.sh",
    "prepublishOnly": "npm run typecheck && npm run build",
    "postinstall": "node scripts/postinstall.cjs",
    "build": "tsc && cp package.json dist/ && cp openclaw.plugin.json dist/ 2>/dev/null || true"
  },
  "keywords": [
    "openclaw",
    "plugin",
    "enhance",
    "memory",
    "safety",
    "agent"
  ],
  "author": "jobzhao15",
  "license": "MIT",
  "peerDependencies": {
    "openclaw": "^2026.4.24"
  },
  "dependencies": {
    "@sinclair/typebox": "^0.34.49",
    "better-sqlite3": "^11.0.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.0",
    "typescript": "^5.5.0"
  },
  "files": [
    "index.ts",
    "openclaw.plugin.json",
    "src/**/*",
    "templates/**/*",
    "scripts/**/*",
    "dist/**/*"
  ],
  "repository": {
    "type": "git",
    "url": "https://cnb.cool/huo15/ai/huo15-openclaw-enhance"
  }
}
