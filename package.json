{
  "name": "@huo15/huo15-openclaw-enhance",
  "version": "6.6.2",
  "publishConfig": {
    "access": "public"
  },
  "description": "火一五·克劳德·龙虾增强插件 v6.6.2 — 上下文守护『runId 去重 + 静音工具』bug fix + UX：(1) P3-13 修 retry 重算 bug — seenRunIds Set 内存去重（bounded 20 FIFO），龙虾 model-fallback retry 时 llm_output 同 runId 二次 emit 跳过累加；(2) P2-10 enhance_ctx_silence({minutes, reason?}) 工具 — 1-60 分钟静音，期间所有 banner（revert/threshold/prediction/budget）跳过，过期自动解除，重启自动解除。enhance_ctx_status 加 mutedUntilMs / seenRunIdsCount 字段。继承 v6.6.0 cost-aware + 多模态精算 + channel 差异化 + v6.5.7 subagent 累加 + 预测式提醒。",
  "type": "module",
  "main": "./dist/index.js",
  "openclaw": {
    "extensions": [
      "./index.ts"
    ],
    "build": {
      "openclawVersion": "2026.4.29"
    },
    "compat": {
      "pluginApi": ">=2026.4.24"
    }
  },
  "bin": {
    "openclaw-enhance-setup": "./scripts/setup.sh"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "release": "bash scripts/release.sh",
    "prepublishOnly": "npm run typecheck && npm run build",
    "postinstall": "node scripts/postinstall.cjs",
    "build": "tsc && cp package.json dist/ && cp openclaw.plugin.json dist/ 2>/dev/null || true"
  },
  "keywords": [
    "openclaw",
    "plugin",
    "enhance",
    "memory",
    "safety",
    "agent"
  ],
  "author": "jobzhao15",
  "license": "MIT",
  "peerDependencies": {
    "openclaw": "^2026.4.24"
  },
  "dependencies": {
    "@sinclair/typebox": "^0.34.49",
    "better-sqlite3": "^11.0.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.0",
    "typescript": "^5.5.0"
  },
  "files": [
    "index.ts",
    "openclaw.plugin.json",
    "src/**/*",
    "templates/**/*",
    "scripts/**/*",
    "dist/**/*"
  ],
  "repository": {
    "type": "git",
    "url": "https://cnb.cool/huo15/ai/huo15-openclaw-enhance"
  }
}
